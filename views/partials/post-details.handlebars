<section class="post">
  <a href="/post/{{id}}">
    <header>{{title}}</header>
  </a>
  <h5>{{format_date post_date}}</h5>
  <h5>{{user.username}}</h5>
  <p>{{content}}</p>
  <br>
  <h6>Comments:</h6>
  {{#each comments as |comment|}}
  <p>{{> comment-details}}</p>
  {{/each}}
</section>